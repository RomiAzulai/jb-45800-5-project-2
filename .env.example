# API server (loaded when running npm run dev / npm run server)
PORT=4174

# Optional: default OpenAI key for local dev if the client does not send X-OpenAI-Api-Key.
# In normal use, users paste their key on the AI Recommendation page only.
OPENAI_API_KEY=

OPENAI_MODEL=gpt-4o-mini

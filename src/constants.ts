export const MAX_SELECTED_COINS = 6;

/** sessionStorage key — user’s OpenAI key for AI recommendations only (never sent to other routes). */
export const SESSION_OPENAI_API_KEY = "cryptonite:user-openai-api-key";
